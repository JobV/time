/ %i.fa.fa-plus.add-project.right{ "ng-click" => "openProjectModal()" }

.row
  %input.project_filter.small-10{ type: 'text', "ng-model" => "search_filter", placeholder: 'Search' }    

.client-n.ng-cloak{ "ng-repeat" => "client in clients | filter:search_filter | limitTo:10" }
  .company-name.ng-cloak{ "ng-bind" => "client.company_name"}
  .uninvoiced
    .money{ "ng-cloak" => "", "ng-bind" => "'€ ' + client.sum_uninvoiced / 100" }
    .time
      {{ parseTime(client.sum_uninvoiced_time) }}

.onepixel

.project-n.ng-cloak{ "ng-repeat" => "project in projects | filter:search_filter | limitTo:10" }
  {{ project.name }}
  %br
  {{ project.hourly_rate / 100 }}





= render 'layouts/new_project'
= render 'layouts/new_client'
