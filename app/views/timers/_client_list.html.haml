.clients{ "ng-controller" => "newClientCtrl" }
  %h3 Clients    
  %i.fa.fa-plus.add-client{ "ng-click" => "modalNewClient()" }

  .client-n{ "ng-repeat" => "client in clients" }
    .client-name
      {{ client.company_name }}

= render 'layouts/new_client'
