%h3 
  CLIENTS
  %i.fa.fa-plus{ "ng-click" => "openModal()" }

.row
  %input.client_filter.small-10{ type: 'text', "ng-model" => "client_filter", placeholder: 'Search clients' }

.client-n{ "ng-repeat" => "client in clients | filter:client_filter | limitTo:10" }
  {{ client.company_name }}

= render 'layouts/new_client'
