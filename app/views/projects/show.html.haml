.container
  .col-md-8.project_list
    %h1= @project.name

    .text-right
      = link_to "Edit project", edit_project_path(@project)

    %h3= "Time logged in #{@project.name}"

  .col-md-3.col-md-offset-1.n_project
    %h3 Delete Project
    = link_to 'Delete', project_path(@project), method: :delete
